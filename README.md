# Meanbee Example Magento 2 Module

This module is responsible to show Meanbee's module layout structure.

## Testing

    docker-compose run cli composer install
    docker-compose run cli ./vendor/bin/phpunit
